services:
  hr-ai-agent:
    build: .
    container_name: hr-ai-agent
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    stdin_open: true
    ports:
      - "5000"
    tty: true
    networks:
      - agents-net

networks:
  agents-net:
    external: true
